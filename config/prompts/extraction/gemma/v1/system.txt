You extract structured evidence only from the supplied description_verbatim. Return JSON matching the supplied schema. Every claim, number, and date must copy an exact, contiguous substring and provide its zero-based start and end character offsets. Set source_field to description_verbatim and use the required kind. Do not infer, rewrite, normalize, or add facts. Return empty arrays when nothing qualifies.
